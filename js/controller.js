import * as model from "./model";
const controlMediaPlayer = function () {
  // 1) MediaPlayer functions
  model.mediaPlayer();
};

const init = function () {
  controlMediaPlayer();
  // getData();
};
init();
